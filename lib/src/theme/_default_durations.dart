part of 'webfabrik_theme.dart';

/// __Default Durations__
///
/// The default miscellaneous values for the [WebfabrikTheme].
class _DefaultDurations {
  const _DefaultDurations();

  static const Duration tiny = Duration(milliseconds: 50);
  static const Duration xTiny = Duration(milliseconds: 100);
  static const Duration xxTiny = Duration(milliseconds: 150);
  static const Duration short = Duration(milliseconds: 200);
  static const Duration xShort = Duration(milliseconds: 250);
  static const Duration xxShort = Duration(milliseconds: 300);
  static const Duration medium = Duration(milliseconds: 400);
  static const Duration xMedium = Duration(milliseconds: 500);
  static const Duration xxMedium = Duration(milliseconds: 600);
}
