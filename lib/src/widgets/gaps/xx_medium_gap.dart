part of 'gaps.dart';

class XXMediumGap extends StatelessWidget {
  const XXMediumGap({super.key});

  @override
  Widget build(BuildContext context) {
    return Gap(WebfabrikTheme.of(context).spacing.xxMedium);
  }
}
