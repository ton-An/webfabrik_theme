part of 'gaps.dart';

class XXLargeGap extends StatelessWidget {
  const XXLargeGap({super.key});

  @override
  Widget build(BuildContext context) {
    return Gap(WebfabrikTheme.of(context).spacing.xxLarge);
  }
}
